apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: review-gateway
  namespace: myistio
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - review.jtthink.com
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: reviewinfo
  namespace: myistio
spec:
  hosts:
    - review.jtthink.com
  gateways:
    - review-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: reviewsvc
            port:
              number: 80

